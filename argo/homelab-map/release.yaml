apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: homelab-map
  namespace: argo-cd
spec:
  destination:
    namespace: homelab-map
    server: 'https://kubernetes.default.svc'
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
    - CreateNamespace=true
  project: default
  source:
    targetRevision: HEAD
    repoURL: 'https://github.com/slashr/homelab-deployments.git'
    path: src/homelab-map
