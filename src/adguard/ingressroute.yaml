apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: adguard-ui-tls
  namespace: adguard
spec:
  secretName: adguard-ui-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - adguard.vind.uk
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: adguard-ui
  namespace: adguard
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  rules:
    - host: adguard.vind.uk
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: adguard-ui
                port:
                  number: 3000
  tls:
    - hosts: [adguard.vind.uk]
      secretName: adguard-ui-tls
